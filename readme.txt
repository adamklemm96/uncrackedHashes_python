This skript is for reading csv data givin back the statistics (how many values in each column)
and is able to export values from second column (hashes) that have no assigned password. 
It means they are are uncracked.
Alway run this skript with an argument (path to a folder where you want to execute it)